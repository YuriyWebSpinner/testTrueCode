## Installation

Use the `npm` or `yarn` package manager to install project.

```bash
cd testTrueCode
npm install
or
yarn install
```

## Usage

Recommended `node.js` version - `12.16.3`


#### Local running

```bash
gulp
```


